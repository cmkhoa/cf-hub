{
  "name": "cf-hub",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install:all": "(cd backend && npm install) && (cd frontend && npm install)",
    "setup": "npm install && npm run install:all",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "echo 'Use frontend/package.json build script instead'",
    "start": "echo 'Use frontend/package.json start script instead'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@datastax/astra-db-ts": "^1.5.0",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "langchain": "^0.3.20",
    "mongodb": "^6.20.0",
    "openai": "^4.91.1",
    "puppeteer": "^24.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
